# Design Review and TODO

最終検討日: 2026-09-21

設計の判断と、その判断を変える条件を残す。実装済みの一覧ではない。
「不具合」「意図された制約」「改善案」を分け、改善案を不具合として扱わない。
優先度は着手順であり、障害の重大度とは別。提案は検証の結果で撤回してよい。

## Principles

- `decode(message)` で使い始められ、`print(report)` で読める入口を保つ。
- 仕様のコード値、未定義値、特殊値を失わない。表示文字列から値を逆算させない。
- 変更が必要な理由を、利用例・仕様・履歴・再現結果のいずれかで示す。
- 公開する概念と、実装だけが必要とする概念を分ける。
- 性能改善は測定して判断する。コピーやクラスの数だけを削減の理由にしない。
- 互換性は関数名だけでなく、出力、属性、例外、状態の寿命、通知の条件まで含む。
- 既存テストが保証する動作と、ドキュメントが約束する動作の食い違いも調べる。
- コードを整理するためだけの一括リライト、一般化、依存ライブラリの追加は行わない。

## Decisions

| ID | 対象 | 現時点の判断 | 優先度 | 次の行動 |
|---|---|---|---|---|
| D01 | ストリームの識別 | 修正・回帰テスト完了 | 完了 | オブジェクト同一性で分離し、解放と再接続時の保持も確認 |
| D02 | 公開契約の整理 | 重複・時刻・辞書出力を文書化済み | 高・一部完了 | 直接生成・属性変更・継承などの互換範囲を決める |
| D03 | デコーダ間の受け渡し | 入力形式 → 共通デコーダの境界を明示・導入済み | 最初の境界は完了 | 下流の schema と Any の受け渡しを次の検討対象にする |
| D04 | 読み取り状態の所有 | 所有者・寿命を調査済み。一括集約はしない | 中 | 共有 reader の排他範囲と明示的解放の契約を決める |
| D05 | 地域ごとのレコード | 利用者向けの追加を条件付きで検討 | 中 | 津波だけで使い方と変更可能性の契約を試す |
| D06 | 等価性・ハッシュ・不変性 | 等価性は維持。重複キーを内部で明示する案を検討 | 中 | 変更可能な値とキャッシュの関係を整理 |
| D07 | 南海トラフ情報の組み立て | 現行の共有仕様を維持。分離は保留 | 低 | 制約を明記し、独立した組み立ての用途が生じたら再検討 |
| D08 | 表示とデータ | `str(report)` を維持。全面分離はしない | 低 | 別言語・別形式の要求が出た箇所で分離を判断 |
| D09 | 設定ミスと受信エラー | 形式の型を分離。例外は互換性のため維持 | 今回分完了 | 例外の再分類は破壊的変更を扱う際に再検討 |

## D01: Stream Identity

分類: 再現済みの不具合。2026-09-20 修正・検証完了。

[StreamKeyedDict](azarashi/streams/state.py) は、修正前はハッシュ可能なストリームを
`WeakKeyDictionary` に入れる。同じハッシュ値と等価性を持つ別のストリームが、
同じキーとして扱われる。ハッシュ不能なストリームだけは `id` で区別している。

独立したストリームの未読文、重複履歴などが混ざる。
以下の再現では、2本目はまだ一文字も読まれていないのに、1本目の DCX が返った。
通常の `StringIO` や pySerial 全般で発生するという指摘ではなく、
値による等価性とハッシュを定義するストリームに限る。

```python
import io
import sys
sys.path.insert(0, 'tests')
import azarashi
from samples import EEW, L_ALERT

class EqualStream(io.StringIO):
    def __eq__(self, other):
        return isinstance(other, EqualStream)

    def __hash__(self):
        return 1

first = EqualStream(EEW + L_ALERT + '\n')
second = EqualStream(EEW + '\n')
azarashi.decode_stream(first, ignore_dcx=False)
report = azarashi.decode_stream(second, ignore_dcx=False)
print(report.message_type, second.tell())  # 修正前: DCX 0。修正後: DCR と読み取り済みの位置
```

- [x] 独立したストリームの未読文が混ざることを再現。
- [x] 状態のキーを、ハッシュ可能性によらずオブジェクト同一性に統一。
- [x] 未読文・重複履歴の独立性、回収後の解放、既存のハッシュ不能・弱参照不能のケースを確認。

弱参照できるオブジェクトと、それ以外の保持を分けた。閉じた弱参照可能なオブジェクトは
回収まで状態を保持し、同じ接続オブジェクトを開き直すときの履歴を維持する。

公開 Reader を導入しても、旧 API の互換経路には残る問題。
大規模な状態管理の変更から切り離して修正する。

## D02: Public Contracts

分類: 契約の明確化。既存動作を自動的に変更する項目ではない。

内部の整理より先に、次の点を確定する。

- [x] `unique=True` は時間では失効しないが、記憶は直近256件に限られると明記。
  [API文書](docs/api.md) の「いつまでも」を、[実装](azarashi/api.py)・
  `test_unique_keeps_the_newest_cache_size_reports` の容量制限と一致させた。
  無制限の記憶へ変更するのではなく、有限の記憶を契約に反映する方針。
- [x] 秒数指定は「最後の通知からの周期」ではなく「最後の受信からの間隔」と明記。
  抑制した電文も時刻を更新する。`be7a859` の意図と一致しており、変更しない。
- [x] 通知中・通知失敗時の重複抑制の保証範囲を明記。
  `1c43aeb` は、コールバック失敗時に次のコピーを通知できるよう修正している。
  既に並行して読み飛ばしたコピーの再配信まで保証しているわけではない。
- [x] 受信時刻が、欠けた年月日の補完と重複の期限の両方に使われることを整理。
  時計の巻き戻り、固定時刻での再生を含めて検討する。
  単調時計への置換は再生時の意味を変えるので、機械的に採用しない。
- [x] `get_params()` の範囲を明記。
  現状はインスタンス辞書の深いコピーで、DCX のクラス属性による `None` の既定値は含まれない。
  `a11_international_library is None` でも、そのキーが辞書にない例を確認済み。
  全属性一覧でも JSON 出力でもない。全フィールドを追加する変更は既存出力を変える。
- [ ] サポートする利用方法を整理。
  レポートの直接生成・属性の変更・サブクラス化・内部デコーダの直接利用の範囲を確認する。
  型検査用の誤用生成ではレポートのコンストラクタは対象外になっている。
  公開モジュール内にあることだけで、変更してよい／いけないとは断定しない。
- [x] 自作ストリームの再接続時の履歴保持は、弱参照可能な場合に保証すると明記（2026-09-21）。
  弱参照不可で `closed` を公開する場合、後の状態検索で閉じていることを確認すると破棄する。
  閉じた瞬間に必ず消えるわけではなく、検索前に再度開けば残る場合もある。保持は保証しない。
  `serial.Serial` を使う文書の再接続例は、この制限に該当しない。
  `test_closed_weakly_referenced_stream_keeps_its_state` と
  `test_stream_keyed_dict_drops_closed_streams_that_cannot_be_weakly_referenced` が両側を確認している。
  解放の仕組みをなくして保持を無制限にする変更は行わず、API 文書と例外の説明を補った。

完了条件: 上記の動作と制約が文書から分かり、必要な契約テストがある。
私有辞書の具体的な実装を固定するテストは、構造変更の際に利用者の動作を確認する形へ移す。

## D03: Decoder Data Flow

分類: 保守性の改善。既存のデコード結果が誤っているという指摘ではない。

現状は `Decoder.get_params()` → `**kwargs` → レポートの生成 → 属性のコピーを
段階ごとに繰り返す。[基底デコーダ](azarashi/decoders/base.py) の `schema` は
必須引数の存在をある程度確認するが、注釈どおりの実行時型検証を行うものではない。
辞書が `Any` を含むため、段階間の不足や誤記を静的に検出しにくい。

比較する方法:

| 方法 | 利点 | 負担・限界 | 判断 |
|---|---|---|---|
| 現状維持＋契約テスト | 最小の変更 | 引き渡す値の全体像は追いにくいまま | 比較基準として残す |
| 明示的な引数・TypedDict | `**kwargs` の構造を活かせる | `Any` の辞書に戻すと効果が消える | 境界が小さい箇所で候補 |
| 小さな内部データ型 | 必要な値と受け渡しが明確 | レポートとの変換や重複が増える可能性 | 共通フレームの境界で候補 |
| 全デコーダ・全レポートの一括変更 | 一様な実装にできる | 型、出力、互換性を同時に変えやすい | 採用しない |

- [x] NMEA/UBX/HEX/net の処理後に共有するデータを列挙する。
  本体、元の電文、受信時刻、衛星情報、NMEA 表現をどの段階で確定するかを決める。
- [x] 一つの境界で必要な構造を検討し、追加クラスを設けない明示的な引数を採用。
- [x] EEW、NULL DCX、警報 DCX が通る共通の境界で、必要値の欠落・型違い・誤記を静的に検出。
- [x] 出力・既存属性・例外の診断情報・時刻の決定時点を既存テストで確認。
- [x] 対象の4箇所では辞書展開を除去。下流全体の型安全化とは区別する。

小さな内部型を追加するだけで、DCR共通項目やDCX最終レポートまでの型安全性が
自動的に解決するわけではない。効果を見てから次の境界を扱う。
コピーのコストを理由に進めない。`4858671` ではコピーのコストを測って許容している。

### Boundary Experiment (2026-09-21)

リポジトリ外の一時コピーで、入力形式のデコーダから `common.Decoder` へ渡す境界だけを試作した。
この試作を経て、下記の範囲で本体に導入した。新しいデータ型を作る前に、明示的な引数だけで
どこまで改善するかを確認する実験。

この境界で必要な値は次のとおり。

| 引数 | 型 | 決定する場所 |
|---|---|---|
| `sentence` | `str \| bytes` | 各形式の既存処理後の電文。完全な未加工入力とは限らない |
| `message` | `bytes` | 各形式から抽出した本体 |
| `nmea` | `str` | 各形式の処理末尾で生成する NMEA 表現 |
| `timestamp` | `datetime` | 入力デコーダの生成時。下流で現在時刻を取り直さない |
| `message_header` | `str \| bytes \| None` | NMEA・UBX で設定。HEX・net は `None` |
| `satellite_id` / `satellite_prn` | `int \| None` | 各形式で設定。情報がない場合は `None` |
| `satellite_svid` | `int \| None` | UBX だけが持つ受信機の衛星番号 |

`raw` は `MessagePartial` が本体から算出し直すため、この境界の引数には不要。
`preamble` と `message_type` は共通デコーダで解析するため、まだ渡さない。

試作の変更は、共通デコーダに型付きのコンストラクタを定義し、4形式の呼び出しを
`**self.get_params()` から必要な引数の列挙に替えるだけ。
必須値は `sentence`・`message`・`nmea`・`timestamp`、衛星情報などは `None` を既定とした。
レポートのクラス、内部 schema 処理、DCR/DCX の次の境界は変更していない。

確認結果:

- フレーム解析、DCR/DCX、時刻、例外、既存ログのゴールデン比較の計183テストが成功。
- mypy strict は成功。Pyright はエラーなし、既存の `serial` ソース解決警告1件。
- 必須の `message` の省略、`message` への文字列指定、`satellite_id` の誤記を、
  両方の型検査がそれぞれ検出した。
- テストの実行前に、一時コピーのパッケージを読み込んでいることを確認した。

判断: 最初の改善案は「共通データ型の追加」から「明示的な引数」に縮小する。
この境界だけなら追加クラスなしで必要な静的検査を得られる。TypedDict や dataclass の
実装比較はまだ行っておらず、それらが将来不要だと結論したわけではない。

### Adoption (2026-09-21)

- [x] 内部の `common.Decoder` に明示的なコンストラクタを導入。
  必須引数を増やし、任意の `**kwargs` は受けない。内部コンストラクタの旧呼び出しとの
  互換性は維持せず、公開 `decode()` の結果を維持する。開発文書に契約を記載。
- [x] 全テストと型検査を実行。3種類の誤用を `tests/typing/decoder_mistakes.py` に固定し、
  CI で両方の型検査器が各行の意図したエラーを検出することを確認する。
- [ ] schema によるレポートの一時生成と、その先の `Any` の受け渡しは残る。
  今回の一境界の改善を、デコーダ全体の型安全化や無副作用化とは扱わない。

## D04: Ownership of Reading State

分類: 内部構造の改善案。公開 Reader を直ちに追加する提案は保留する。

状態はバッファ、未配信の文、重複履歴、配信中の予約、ロックに分かれる。
これらは寿命も異なる。切断で読みかけは捨てても、同じ接続オブジェクトでの再接続では
重複履歴を保持するという既存契約がある。

| 方法 | 判断理由 |
|---|---|
| 私有の受信状態オブジェクトに集約 | 公開 API を増やさず所有者を整理できる。まず比較する |
| 公開 Reader を追加 | 同じストリームに2個作ったときの読み取り・重複履歴・ロックの扱いが新たな問題になる |
| フレーム解析を `feed(bytes)` に分離 | 任意の I/O と組み合わせやすいが、現在の逐次読込・復旧の作り直しが必要。具体的な用途まで保留 |
| ジェネレータだけに置換 | 未処理の例外が外へ出ると再開できず、タイムアウト後の再読込を単純には置き換えられない |

- [x] 状態の所有者と、切断・EOF・再接続・GCでの扱いを表にする。
  [調査と設計判断](docs/design/reading-state.md) に実装・テスト・一時的な再現確認を記録。
  弱参照不可なストリームでは、`closed=True` の確認による解放と、再接続をまたぐ保持を
  自動では両立できない。`closed` 自体がない場合の解放手段も、明示的な所有者を検討する際に扱う。
- [x] `decode_stream()` が使うストリームと、`ReaderStore` が使う束縛メソッドの所有者が
  異なる場合を整理する。テキストラッパーの `.buffer` などを含む。
- [ ] 集約した値からキーのストリームを強参照し、弱参照による解放を妨げないか確認。
- [ ] コールバックをロック外で呼ぶこと、通知成功後の重複確定、並行読込の保証を維持する。

2026-09-21 の判断: 一括集約は見送る。通知状態と抽出状態は所有者・寿命が異なる。
API の stream ごとの通知状態と、実際の読み取り元ごとの抽出状態の二層を候補とする。
実装変更の前に次を決める。

- [x] 同じ `.buffer` を共有する `decode_stream()` 呼び出しの並行読み取りを修正。
  部分ヘッダの読み取り中に別ラッパーが残りを消費して電文が欠落することを再現。
  実際の reader の所有者ごとに抽出用ロックを追加。通知履歴は外側に残す。
  内側を直接渡す呼び出しとの排他、履歴の分離、コールバック中の読み取りも検証。
- [x] 同じ所有者での読み取りメソッド・形式の切り替え契約を、リセットの設計案に含めた。
  抽出用ロックは共有しても `read` / `read1` の先読み状態は別なので、切り替えで未配信データを引き継げない。
- [x] 束縛されていない弱参照可能な callable の保持を改善する。
  `StreamKeyedDict` で同一性による弱参照管理に変更。利用中の未配信文は残し、利用終了後は
  クロージャが参照するストリームも回収されることを確認。修正前に回帰テストが失敗することも確認。
- [ ] 弱参照できず closed もない所有者・callable の明示的解放を検討する。
  強参照による保持は残る。今回の修正で全 reader の自動解放を保証するわけではない。
- [x] 切断例外を観測しない close/open と、未配信データの扱いの設計案を決めた。
  [再接続リセットの設計案](docs/design/reconnect-reset.md): 部分データと抽出済み未配信電文を
  実際の所有者単位で破棄し、履歴・予約・ロックは維持する。共有元を使う読み取り・通知の終了が前提。
  公開 Reader は追加せず、小さな明示操作を候補にする。未実装・未公開。
- [ ] 所有者単位の抽出状態破棄を実装・検証し、リセット操作を公開 API に接続する。
  共有 buffer への影響、履歴の保持、別所有者の独立性、繰り返し操作を検証する。
  I/O の受信バッファ消去や実行中の通知取り消しは対象外。詳細は設計案の受入条件を参照。

公開 Reader の再検討条件: 明示的なリセットや非同期入力など、現 API では扱いにくい
具体的な利用例があり、互換経路と共存しても全体の説明と実装が簡単になること。

## D05: Related Values

分類: 利用者向けの改善案。津波の地域・高さ・到達時刻が別リストになっている。
[Tsunami](azarashi/reports/dcr.py) の添字の対応を一つのレコードで表す利点はある。

- [ ] 津波に限定し、`report.forecasts` のような読み取り用のまとまりを比較する。
- [ ] 既存のリストからその都度作るスナップショットを最初の候補にする。
  リストとレコードを両方保存して同期する実装は採用しない。
- [ ] 元のリストの変更、長さ不整合、返した値の変更が何に影響するかを決める。
  `zip()` で短いリストに黙って合わせ、情報を落とす設計は避ける。
- [ ] コード値・未定義値・到達済み・情報なしをそのまま表現する。
  高さや震度を一律に数値へ変換しない。

採用条件: 代表的な利用コードが読みやすくなり、利用者が覚える概念が増えすぎないこと。
DCX の楕円や地域への展開は、この結果を見てから判断する。
プロパティ追加でも、公開メンバーを列挙する型検査・文書テストは更新が必要。

## D06: Equality and Mutation

分類: 契約と内部実装の整理。`==` の変更と全面的な不変化は行わない。

現在の同値性は「同じ具象クラス、同じ raw」。衛星や受信時刻を比較しないのは、
複数衛星から届く同じ電文を扱うための合理的な意味づけ。
新しい重複キーを追加するために、この意味を壊す必要はない。

- [ ] 重複判定の内部キーを `(type(report), report.raw)` として明示する案を比較する。
  この値は不変でも、元の `report.raw` は変更可能なので、取得時点を決める必要がある。
- [ ] キャッシュにレポートそのものを保存する必要を検討。
  キーと最終受信時刻だけを保存する場合も、容量・順序・失敗時の通知を維持する。
- [ ] コールバックが `raw` や `timestamp` を変更した場合を契約の対象にするか決める。
- [ ] ハッシュ化されたレポートの `raw` を変更できる問題を整理する。
  内部キーの導入だけでは、利用者が set/dict に入れる場合の問題は解決しない。
  変更非対応と文書化する、識別部分を変更不可にする、次の破壊的変更で非ハッシュ化する案を比較。

`frozen=True` の付与だけでは、内部のリスト・辞書も変更不可にはならない。
全面的な不変化は、既存の属性操作やレポート生成方法を含む別の API 設計として扱う。

## D07: Nankai Assembly

分類: 合意した仕様。現時点で修正対象にしない。

全受信元が最新の発表の組み立て状態を共有する。
過去のレポートの本文と `completed` が共有状態に依存することを明記する。

- [ ] ライブ受信と古い記録の独立した再生は保証しないと文書化する。
- [ ] 独立再生や複数の組み立てが必要になった場合だけ、明示的な組み立て器を検討する。

単にクラス変数を既定の共有オブジェクトへ移しても、共有状態の性質は変わらない。
組み立て器を作るなら、何を共有するかを利用者が選べることを導入理由とする。
`decode()` の無副作用化を美観だけの理由で押し通さない。

## D08: Rendering

分類: 現行維持。

日本語で読めるレポートは、このライブラリの提供する機能。
`__str__` があること自体を責務の混在とは扱わない。
別言語、時差指定、JSON等の明確な用途が現れたら、共通の表示関数に委譲する案を検討する。
その場合も `str(report)` は入口として残す。

名称表と仕様上の特別値を一律の Enum や浮動小数点数へ置き換えない。
現在の定義表は、未定義コードへの対応にも役割がある。

## D09: Configuration Errors

分類: 再現済みの動作に対する契約上の検討事項。

`decode_stream(stream, msg_type='net')` は `AzarashiInvalidMessageError`、すなわち
`AzarashiReadOn` を送出する。しかし、入力を消費せず、同じ呼び出しでは解消しない。
`readline()` のない入力なども同様。例外の「次を読む」という説明と合わない。
修正前は共通の `MessageFormat` が `net` を許す一方、ストリーム API は許していなかった。

- [x] `net` 指定で、入力位置が0のまま `AzarashiReadOn` 系の例外になることを確認。
- [x] `StreamFormat` を分離し、`decode_stream()` と `Transmitter.start()` の `net` 指定を静的に検出。
- [x] 設定ミスを通常の `ValueError` / `TypeError` 等に分ける案と、既存の捕捉との互換性を比較。
  `test_everything_raised_is_caught_by_the_earlier_names` が旧例外による捕捉を保証している。
  分類の変更はその互換性を壊すため今回は維持し、再試行では解消しない条件を API 文書に明記した。
- [x] 非対応の電文と非対応の API 指定を文書で区別。前者は受信を続けられる。

型検査を使わない利用者の誤指定で再試行を繰り返せる点は残る。解消済みとは扱わない。
新しい例外による再分類は、旧例外の捕捉との互換性を変更する際の検討項目とする。

入力不正の分類は既存の例外契約にも影響するため、内部整理に紛れて変更しない。

## Order of Work

1. D01 は修正・検証済み。
2. D02 の残りの互換範囲を整理する。D09 は型を修正し、実行時例外の分類は維持する判断を記録済み。
3. D03 の最初の境界は導入済み。下流への展開は schema の一時生成を含めて別途検討する。
4. D04 と D06 の内部状態を検討する。公開 API を増やすかは、その後に判断する。
5. D05 を利用例で評価する。D07 と D08 は必要性が生じるまで維持する。

新しく見つかった問題は、このファイルに再現条件・影響・判断・次の行動を追加する。
実装時は既存の電文・復旧・重複・型・出力比較のうち関連する検証を行う。
ゴールデン出力が変わるときは、意図した契約変更かどうかを先に判断し、機械的に更新しない。

## Validation

2026-09-20 の D01・D02・D09 に関する変更:

- 全1,525テスト成功。電文出力のゴールデンファイルは変更していない。
- Ruff、mypy 成功。Pyright はエラーなし、環境の `serial` ソース解決警告1件。
- 正しい API 利用は型検査を通過。手書きの誤用6件と生成した誤用53件の検出を確認。
- 未読電文と重複履歴の混線は、修正前に回帰テストが失敗することを確認済み。

2026-09-21 の D03 最初の境界の導入:

- 全1,525テスト成功。ゴールデンファイルの更新なし。
- Ruff、mypy strict 成功。Pyright はエラーなし、既存の `serial` ソース解決警告1件。
- 内部境界の誤用3件と既存の生成誤用53件を、両方の型検査器がすべて検出。

D04 の関数 reader の保持修正:

- 全1,529テスト成功（回帰テスト4件追加）。Ruff、mypy strict 成功。
- Pyright はエラーなし、既存の `serial` ソース解決警告1件。

D04 の共有読み取り元の排他修正:

- 全1,533テスト成功（回帰テスト4ケース追加）。修正前の電文欠落を確認済み。
- Ruff、mypy strict 成功。Pyright はエラーなし、既存の `serial` ソース解決警告1件。
