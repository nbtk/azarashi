qzss_dcr_jma_latitude_degrees = range(0, 90)
qzss_dcr_jma_longitude_degrees = range(0, 180)
qzss_dcr_jma_latitude_and_longitude_minutes = range(0, 60)
qzss_dcr_jma_latitude_and_longitude_seconds = range(0, 60)

# names the whole LatLon field (41 bits) as one number when a part of it is outside its range
qzss_dcr_jma_latitude_and_longitude_undefined = "緯度・経度(コード番号：%d)"
